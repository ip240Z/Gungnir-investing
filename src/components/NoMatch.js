const NoMatch = () => {

    let style = {
        width: "300px",
        color: "white",
    }

    return (
        <section style={style}>
            <h3>We do not have that page.</h3>
        </section>
    )
}

export default NoMatch